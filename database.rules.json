{
  "rules": {
    "users": {
      "$user_id": {
        ".read": "auth != null && $user_id == auth.uid",
        ".write": "auth != null && $user_id == auth.uid"
      }
    },
    "courses": {
      ".read": "auth != null",
      "$course_id": {
        ".read": "auth != null",
        ".write": "!data.exists() || data.child('members').child(auth.uid).exists()"
      }
    }
  }
}
