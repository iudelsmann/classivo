/**
 * Interface to represent a Class (as in a course).
 *
 * @export
 * @interface Class
 */
export interface Class {
  /**
   * The name of class.
   *
   * @type {string}
   * @memberof Class
   */
  name: string;
}
